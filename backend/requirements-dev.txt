# Development-only dependencies
# Install with: pip install -r requirements-dev.txt
# (Production installs only requirements.txt)

# Code quality
black>=24.0.0
flake8>=7.0.0
flake8-bugbear>=24.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx>=0.27.0

# Pre-commit hooks
pre-commit>=3.7.0
